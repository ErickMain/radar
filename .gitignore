# Secrets — nunca commitar
.env

# Perfis de Chrome de tentativas anteriores de automação local (retirado —
# ver histórico do git). Se algo recriar essas pastas, guardam sessão do
# LinkedIn (cookies) e jamais devem ser commitadas.
.chrome-automation-profile/
.playwright-profile/

# Currículo — arquivo pessoal, não vai pro repo público
assets/curriculo.pdf
assets/resume_en.pdf

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# OS
.DS_Store
Thumbs.db
